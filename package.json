{
	"name": "chat-app",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"build": "cd client && yarn build",
		"install-client": "cd client && yarn install",
		"heroku-client": "yarn install-client && yarn build"
	},
	"license": "MIT",
	"dependencies": {
		"express": "^4.17.1",
		"socket.io": "^2.3.0"
	}
}
